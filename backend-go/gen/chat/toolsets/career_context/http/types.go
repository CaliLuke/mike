// Code generated by Loom, DO NOT EDIT.
//
// career_context tool transport types
//
// Command:
// $ loom gen github.com/CaliLuke/luke/backend-go/design

package http

type (
	// CreateApplicationPayloadTransport is the internal JSON transport type for CreateApplicationPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	CreateApplicationPayloadTransport struct {
		// Application name, usually the role title from the job ad
		Name *string `json:"name,omitempty"`
		// Local company identifier returned by create_company or a prior
		// company_created event
		CompanyID *string `json:"company_id,omitempty"`
		// Optional full job description text to save as an application document
		JobDescriptionText *string `json:"job_description_text,omitempty"`
		// Optional source URL for the job description
		JobDescriptionURL *string `json:"job_description_url,omitempty"`
	}

	// CreateApplicationResultTransport is the internal JSON transport type for CreateApplicationResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	CreateApplicationResultTransport struct {
		// Whether the application was created
		OK *bool `json:"ok,omitempty"`
		// Local application identifier
		ApplicationID *string `json:"application_id,omitempty"`
		// Attached local company identifier
		CompanyID *string `json:"company_id,omitempty"`
		// Application name
		Name *string `json:"name,omitempty"`
		// Created job description document identifier, when job text was provided
		JobDescriptionDocumentID *string `json:"job_description_document_id,omitempty"`
		// Error message when creation failed
		Error *string `json:"error,omitempty"`
	}

	// CreateCompanyPayloadTransport is the internal JSON transport type for CreateCompanyPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	CreateCompanyPayloadTransport struct {
		// Company name
		Name *string `json:"name,omitempty"`
		// Optional company website
		Website *string `json:"website,omitempty"`
		// Set true only after reviewing a similar existing company and deciding a
		// separate company should be created
		ConfirmNew *bool `json:"confirm_new,omitempty"`
	}

	// CreateCompanyResultTransport is the internal JSON transport type for CreateCompanyResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	CreateCompanyResultTransport struct {
		// Whether the company was created
		OK *bool `json:"ok,omitempty"`
		// Local company identifier
		CompanyID *string `json:"company_id,omitempty"`
		// Company name
		Name *string `json:"name,omitempty"`
		// Company website
		Website *string `json:"website,omitempty"`
		// Whether an existing company was reused instead of creating a duplicate
		ReusedExisting *bool `json:"reused_existing,omitempty"`
		// Whether a similar existing company blocked creation until confirm_new is true
		RequiresConfirmation *bool `json:"requires_confirmation,omitempty"`
		// Similar existing company identifier, when one was found
		SimilarCompanyID *string `json:"similar_company_id,omitempty"`
		// Similar existing company name, when one was found
		SimilarCompanyName *string `json:"similar_company_name,omitempty"`
		// Similarity score for the nearest existing company, from 0 to 1
		Similarity *float64 `json:"similarity,omitempty"`
		// Error message when creation failed
		Error *string `json:"error,omitempty"`
	}

	// EditDocumentPayloadTransport is the internal JSON transport type for EditDocumentPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	EditDocumentPayloadTransport struct {
		// Local document identifier to edit
		DocumentID *string `json:"document_id,omitempty"`
		// Text replacement edits
		Edits []*AssistantDocumentEditTransport `json:"edits,omitempty"`
	}

	// EditDocumentResultTransport is the internal JSON transport type for EditDocumentResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	EditDocumentResultTransport struct {
		// Whether edits were applied
		OK *bool `json:"ok,omitempty"`
		// Document filename
		Filename *string `json:"filename,omitempty"`
		// Local download URL
		DownloadURL *string `json:"download_url,omitempty"`
		// Local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// New document version identifier
		VersionID *string `json:"version_id,omitempty"`
		// New version number
		VersionNumber *int `json:"version_number,omitempty"`
		// Edit annotations
		Annotations []*AssistantEditAnnotationTransport `json:"annotations,omitempty"`
		// Error message when editing failed
		Error *string `json:"error,omitempty"`
	}

	// FetchDocumentsPayloadTransport is the internal JSON transport type for FetchDocumentsPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FetchDocumentsPayloadTransport struct {
		// Local document identifiers to fetch
		DocumentIds []string `json:"document_ids,omitempty"`
	}

	// FetchDocumentsResultTransport is the internal JSON transport type for FetchDocumentsResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FetchDocumentsResultTransport struct {
		// Fetched document contents
		Documents []*AssistantDocumentTextTransport `json:"documents,omitempty"`
	}

	// FetchWebPagePayloadTransport is the internal JSON transport type for FetchWebPagePayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FetchWebPagePayloadTransport struct {
		// Public HTTP or HTTPS URL to download and simplify
		URL *string `json:"url,omitempty"`
		// Optional maximum characters of simplified text to return
		MaxChars *int `json:"max_chars,omitempty"`
	}

	// FetchWebPageResultTransport is the internal JSON transport type for FetchWebPageResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FetchWebPageResultTransport struct {
		// Final fetched URL after redirects
		URL *string `json:"url,omitempty"`
		// Extracted page title
		Title *string `json:"title,omitempty"`
		// Simplified readable page text
		Text *string `json:"text,omitempty"`
		// Whether the simplified text was truncated
		Truncated *bool `json:"truncated,omitempty"`
		// Error message when fetching failed
		Error *string `json:"error,omitempty"`
	}

	// FindInDocumentPayloadTransport is the internal JSON transport type for FindInDocumentPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FindInDocumentPayloadTransport struct {
		// Local document identifier to search
		DocumentID *string `json:"document_id,omitempty"`
		// Exact text to find
		Query *string `json:"query,omitempty"`
		// Maximum number of matches to return
		MaxResults *int `json:"max_results,omitempty"`
		// Characters of context to include around each match
		ContextChars *int `json:"context_chars,omitempty"`
	}

	// FindInDocumentResultTransport is the internal JSON transport type for FindInDocumentResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FindInDocumentResultTransport struct {
		// Local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// Document filename
		Filename *string `json:"filename,omitempty"`
		// Search matches
		Matches []*AssistantDocumentMatchTransport `json:"matches,omitempty"`
		// Total matches found before truncation
		TotalMatches *int `json:"total_matches,omitempty"`
	}

	// GenerateDocxPayloadTransport is the internal JSON transport type for GenerateDocxPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	GenerateDocxPayloadTransport struct {
		// Document title
		Title *string `json:"title,omitempty"`
		// Whether to use landscape orientation
		Landscape *bool `json:"landscape,omitempty"`
		// Document sections
		Sections []*AssistantDocxSectionTransport `json:"sections,omitempty"`
	}

	// GenerateDocxResultTransport is the internal JSON transport type for GenerateDocxResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	GenerateDocxResultTransport struct {
		// Whether the document was generated
		OK *bool `json:"ok,omitempty"`
		// Generated filename
		Filename *string `json:"filename,omitempty"`
		// Local download URL
		DownloadURL *string `json:"download_url,omitempty"`
		// Local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// Current document version identifier
		VersionID *string `json:"version_id,omitempty"`
		// Current version number
		VersionNumber *int `json:"version_number,omitempty"`
		// Error message when generation failed
		Error *string `json:"error,omitempty"`
	}

	// ListDocumentsPayloadTransport is the internal JSON transport type for ListDocumentsPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ListDocumentsPayloadTransport struct {
	}

	// ListDocumentsResultTransport is the internal JSON transport type for ListDocumentsResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ListDocumentsResultTransport struct {
		// Documents available to the current chat
		Documents []*AssistantDocumentRefTransport `json:"documents,omitempty"`
	}

	// ListWorkflowsPayloadTransport is the internal JSON transport type for ListWorkflowsPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ListWorkflowsPayloadTransport struct {
	}

	// ListWorkflowsResultTransport is the internal JSON transport type for ListWorkflowsResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ListWorkflowsResultTransport struct {
		// Saved workflows
		Workflows []*AssistantWorkflowRefTransport `json:"workflows,omitempty"`
	}

	// ReadDocumentPayloadTransport is the internal JSON transport type for ReadDocumentPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReadDocumentPayloadTransport struct {
		// Local document identifier to read
		DocumentID *string `json:"document_id,omitempty"`
	}

	// ReadDocumentResultTransport is the internal JSON transport type for ReadDocumentResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReadDocumentResultTransport struct {
		// Local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// Document filename
		Filename *string `json:"filename,omitempty"`
		// Plain text extracted from the current document version
		Text *string `json:"text,omitempty"`
	}

	// ReadTableCellsPayloadTransport is the internal JSON transport type for ReadTableCellsPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReadTableCellsPayloadTransport struct {
		// Tabular review identifier
		ReviewID *string `json:"review_id,omitempty"`
		// Column positions to read; omit for all
		ColIndices []int `json:"col_indices,omitempty"`
		// Row positions to read; omit for all
		RowIndices []int `json:"row_indices,omitempty"`
	}

	// ReadTableCellsResultTransport is the internal JSON transport type for ReadTableCellsResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReadTableCellsResultTransport struct {
		// Human-readable row/column count
		Label *string `json:"label,omitempty"`
		// Readable cell content
		Text *string `json:"text,omitempty"`
	}

	// ReadWorkflowPayloadTransport is the internal JSON transport type for ReadWorkflowPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReadWorkflowPayloadTransport struct {
		// Local workflow identifier to read
		WorkflowID *string `json:"workflow_id,omitempty"`
	}

	// ReadWorkflowResultTransport is the internal JSON transport type for ReadWorkflowResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReadWorkflowResultTransport struct {
		// Local workflow identifier
		WorkflowID *string `json:"workflow_id,omitempty"`
		// Workflow title
		Title *string `json:"title,omitempty"`
		// Workflow type
		Type *string `json:"type,omitempty"`
		// Workflow prompt
		PromptMd *string `json:"prompt_md,omitempty"`
		// Workflow column configuration
		ColumnsConfig []any `json:"columns_config,omitempty"`
		// Workflow practice or category
		Practice *string `json:"practice,omitempty"`
	}

	// ReplicateDocumentPayloadTransport is the internal JSON transport type for ReplicateDocumentPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReplicateDocumentPayloadTransport struct {
		// Local document identifier to copy
		DocumentID *string `json:"document_id,omitempty"`
		// Number of copies to create
		Count *int `json:"count,omitempty"`
		// Optional filename for a single copy
		NewFilename *string `json:"new_filename,omitempty"`
	}

	// ReplicateDocumentResultTransport is the internal JSON transport type for ReplicateDocumentResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReplicateDocumentResultTransport struct {
		// Whether replication succeeded
		OK *bool `json:"ok,omitempty"`
		// Source filename
		Filename *string `json:"filename,omitempty"`
		// Number of copies created
		Count *int `json:"count,omitempty"`
		// Created copies
		Copies []*AssistantDocumentCopyTransport `json:"copies,omitempty"`
		// Error message when replication failed
		Error *string `json:"error,omitempty"`
	}

	// SetApplicationCompanyPayloadTransport is the internal JSON transport type for SetApplicationCompanyPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	SetApplicationCompanyPayloadTransport struct {
		// Local application identifier to move
		ApplicationID *string `json:"application_id,omitempty"`
		// Existing local company identifier to attach. Mutually exclusive with
		// company_name.
		CompanyID *string `json:"company_id,omitempty"`
		// Free-text company name. When set, the tool resolves an existing matching
		// company or creates a new one — preferred when the assistant has just
		// identified the company from materials.
		CompanyName *string `json:"company_name,omitempty"`
		// When using company_name and a similar company already exists, set true to
		// bypass the dedupe warning and create a fresh row.
		ConfirmNew *bool `json:"confirm_new,omitempty"`
	}

	// SetApplicationCompanyResultTransport is the internal JSON transport type for SetApplicationCompanyResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	SetApplicationCompanyResultTransport struct {
		// Whether the application's company was updated
		OK *bool `json:"ok,omitempty"`
		// Local application identifier
		ApplicationID *string `json:"application_id,omitempty"`
		// Now-attached local company identifier
		CompanyID *string `json:"company_id,omitempty"`
		// Now-attached company name
		CompanyName *string `json:"company_name,omitempty"`
		// Previously attached company identifier
		PreviousCompanyID *string `json:"previous_company_id,omitempty"`
		// Previously attached company name
		PreviousCompanyName *string `json:"previous_company_name,omitempty"`
		// Whether a similar existing company blocked creation until confirm_new is true
		RequiresConfirmation *bool `json:"requires_confirmation,omitempty"`
		// Similar existing company identifier, when one was found
		SimilarCompanyID *string `json:"similar_company_id,omitempty"`
		// Similar existing company name, when one was found
		SimilarCompanyName *string `json:"similar_company_name,omitempty"`
		// Similarity score for the nearest existing company, from 0 to 1
		Similarity *float64 `json:"similarity,omitempty"`
		// Error message when the update failed
		Error *string `json:"error,omitempty"`
	}

	// AssistantDocumentCopyTransport is the internal JSON transport type for AssistantDocumentCopyTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantDocumentCopyTransport struct {
		// Copied document filename
		NewFilename *string `json:"new_filename,omitempty"`
		// New local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// New version identifier
		VersionID *string `json:"version_id,omitempty"`
		// Local download URL
		DownloadURL *string `json:"download_url,omitempty"`
	}

	// AssistantDocumentEditTransport is the internal JSON transport type for AssistantDocumentEditTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantDocumentEditTransport struct {
		// Exact text to replace
		Find *string `json:"find,omitempty"`
		// Replacement text
		Replace *string `json:"replace,omitempty"`
		// Optional expected text before the match
		ContextBefore *string `json:"context_before,omitempty"`
		// Optional expected text after the match
		ContextAfter *string `json:"context_after,omitempty"`
		// Reason for the change
		Reason *string `json:"reason,omitempty"`
	}

	// AssistantDocumentMatchTransport is the internal JSON transport type for AssistantDocumentMatchTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantDocumentMatchTransport struct {
		// Zero-based match index
		Index *int `json:"index,omitempty"`
		// Start character offset
		Start *int `json:"start,omitempty"`
		// End character offset
		End *int `json:"end,omitempty"`
		// Matched text
		Quote *string `json:"quote,omitempty"`
		// Surrounding text context
		Context *string `json:"context,omitempty"`
	}

	// AssistantDocumentRefTransport is the internal JSON transport type for AssistantDocumentRefTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantDocumentRefTransport struct {
		// Local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// Document filename
		Filename *string `json:"filename,omitempty"`
		// Owning application identifier, when attached to a application
		ApplicationID *string `json:"application_id,omitempty"`
		// Stored file type or MIME hint
		FileType *string `json:"file_type,omitempty"`
		// Processing status
		Status *string `json:"status,omitempty"`
	}

	// AssistantDocumentTextTransport is the internal JSON transport type for AssistantDocumentTextTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantDocumentTextTransport struct {
		// Local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// Document filename
		Filename *string `json:"filename,omitempty"`
		// Plain text extracted from the current document version
		Text *string `json:"text,omitempty"`
	}

	// AssistantDocxSectionTransport is the internal JSON transport type for AssistantDocxSectionTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantDocxSectionTransport struct {
		// Optional section heading
		Heading *string `json:"heading,omitempty"`
		// Heading level: 1, 2, or 3
		Level *int `json:"level,omitempty"`
		// Section prose; blank lines split paragraphs
		Content *string `json:"content,omitempty"`
		// Whether to start this section on a new page
		PageBreak *bool `json:"pageBreak,omitempty"`
		// Optional table for this section
		Table *AssistantDocxTableTransport `json:"table,omitempty"`
	}

	// AssistantDocxTableTransport is the internal JSON transport type for AssistantDocxTableTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantDocxTableTransport struct {
		// Table header cells
		Headers []string `json:"headers,omitempty"`
		// Table body rows
		Rows [][]string `json:"rows,omitempty"`
	}

	// AssistantEditAnnotationTransport is the internal JSON transport type for AssistantEditAnnotationTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantEditAnnotationTransport struct {
		// Annotation kind
		Kind *string `json:"kind,omitempty"`
		// Edit identifier
		EditID *string `json:"edit_id,omitempty"`
		// Local document identifier
		DocumentID *string `json:"document_id,omitempty"`
		// Document version identifier
		VersionID *string `json:"version_id,omitempty"`
		// Document version number
		VersionNumber *int `json:"version_number,omitempty"`
		// Change identifier
		ChangeID *string `json:"change_id,omitempty"`
		// Text removed
		DeletedText *string `json:"deleted_text,omitempty"`
		// Text inserted
		InsertedText *string `json:"inserted_text,omitempty"`
		// Text before the edit
		ContextBefore *string `json:"context_before,omitempty"`
		// Text after the edit
		ContextAfter *string `json:"context_after,omitempty"`
		// Reason for the change
		Reason *string `json:"reason,omitempty"`
		// Edit status
		Status *string `json:"status,omitempty"`
	}

	// AssistantWorkflowRefTransport is the internal JSON transport type for AssistantWorkflowRefTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	AssistantWorkflowRefTransport struct {
		// Local workflow identifier
		WorkflowID *string `json:"workflow_id,omitempty"`
		// Workflow title
		Title *string `json:"title,omitempty"`
		// Workflow type
		Type *string `json:"type,omitempty"`
		// Workflow practice or category
		Practice *string `json:"practice,omitempty"`
	}
)
