// Code generated by Loom, DO NOT EDIT.
//
// application_chat endpoints
//
// Command:
// $ loom gen github.com/CaliLuke/luke/backend-go/design

package applicationchat

import (
	"context"

	loom "github.com/CaliLuke/loom/pkg"
)

// Endpoints wraps the "application_chat" service endpoints.
type Endpoints struct {
	Stream loom.Endpoint
}

// StreamEndpointInput holds both the payload and the server stream of the
// "stream" method.
type StreamEndpointInput struct {
	// Payload is the method payload.
	Payload *StreamPayload
	// Stream is the server stream used by the "stream" method to send data.
	Stream StreamServerStream
}

// NewEndpoints wraps the methods of the "application_chat" service with
// endpoints.
func NewEndpoints(s Service) *Endpoints {
	return &Endpoints{
		Stream: NewStreamEndpoint(s),
	}
}

// Use applies the given middleware to all the "application_chat" service
// endpoints.
func (e *Endpoints) Use(m func(loom.Endpoint) loom.Endpoint) {
	e.Stream = m(e.Stream)
}

// NewStreamEndpoint returns an endpoint function that calls the method
// "stream" of service "application_chat".
func NewStreamEndpoint(s Service) loom.Endpoint {
	return func(ctx context.Context, req any) (any, error) {
		ep := req.(*StreamEndpointInput)
		return nil, s.Stream(ctx, ep.Payload, ep.Stream)
	}
}
