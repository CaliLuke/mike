# Reference Implementations

This directory holds retired implementations that are useful for comparison but
are not active runtime packages.

- `express-backend/`: the original TypeScript/Express backend. Keep it as a
  read-only compatibility reference while the Loom Go backend is completed.

Normal local development uses `backend-go/` and `frontend/`. Do not add new
product behavior under `reference/`.
